services:
  - type: web
    name: hasta-anketi-api
    env: docker
    region: frankfurt
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      # - key: ConnectionStrings__DefaultConnection
      #   value: your_production_connection_string_here # Update this securely in Render dashboard
